# =============================================================================
# Kustomization for Monitoring Stack
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: monitoring

namespace: monitoring

# Common labels
commonLabels:
  app.kubernetes.io/part-of: monitoring-stack

# Resources
resources:
  - namespace.yaml
  - grafana-dashboards-configmap.yaml

# Helm chart references (for Argo CD)
# Note: Prometheus/Grafana are typically deployed via Helm
# The values.yaml files are used with helm install/upgrade

---
# Namespace definition is inline for simplicity
